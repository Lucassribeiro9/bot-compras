version: '3.8'

services:
    bot-compras:
        build: .
        # imagem para subir no dockerhub
        image: ${USER_DOCKERHUB}/bot-compras:latest
        container_name: bot-compras
        env_file: .env
        volumes:
            - .:/app
        